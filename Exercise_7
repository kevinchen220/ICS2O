float x=10;
float y=10;
float c=0;
void setup() {
  size(320, 600);
  background(255, 255, 255);
}
void draw() {
  while (y<height/2+20) {
    while (x<width) {
      fill(c, c, c);
      ellipse(x, y, 20, 20);

      x+=20;
      c+=1;
    }
    x=10;
    y+=20;
  }
}
